# 环境变量从 .env 加载

services:
  app:
    build: .
    container_name: csi-back
    env_file: .env
    ports:
      - "8000:8080"
    restart: unless-stopped

  # mariadb:
  #   image: mariadb:11
  #   container_name: csi-mariadb
  #   environment:
  #     MYSQL_ROOT_PASSWORD: rootpass
  #     MYSQL_DATABASE: dbname
  #     MYSQL_USER: user
  #     MYSQL_PASSWORD: password
  #   volumes:
  #     - mariadb_data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"
  #   healthcheck:
  #     test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped

  # mongodb:
  #   image: mongo:7
  #   container_name: csi-mongodb
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: admin
  #     MONGO_INITDB_ROOT_PASSWORD: password
  #   volumes:
  #     - mongodb_data:/data/db
  #   ports:
  #     - "27017:27017"
  #   restart: unless-stopped

  # redis:
  #   image: redis:7-alpine
  #   container_name: csi-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   restart: unless-stopped

  # elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
  #   container_name: csi-elasticsearch
  #   environment:
  #     discovery.type: single-node
  #     xpack.security.enabled: "true"
  #     ELASTICSEARCH_INITIAL_ADMIN_PASSWORD: password
  #     ES_JAVA_OPTS: "-Xms512m -Xmx512m"
  #   volumes:
  #     - elasticsearch_data:/usr/share/elasticsearch/data
  #   ports:
  #     - "9200:9200"
  #     - "9300:9300"
  #   healthcheck:
  #     test: ["CMD-SHELL", "curl -s -f -u elastic:password http://localhost:9200/_cluster/health?wait_for_status=yellow || exit 1"]
  #     interval: 15s
  #     timeout: 10s
  #     retries: 10
  #   restart: unless-stopped

  # rabbitmq:
  #   image: rabbitmq:3-management-alpine
  #   container_name: csi-rabbitmq
  #   environment:
  #     RABBITMQ_DEFAULT_USER: rabbitmq
  #     RABBITMQ_DEFAULT_PASS: password
  #     RABBITMQ_DEFAULT_VHOST: /
  #   volumes:
  #     - rabbitmq_data:/var/lib/rabbitmq
  #   ports:
  #     - "5672:5672"
  #     - "15672:15672"
  #   healthcheck:
  #     test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped

# volumes:
#   mariadb_data:
#   mongodb_data:
#   redis_data:
#   elasticsearch_data:
#   rabbitmq_data:
