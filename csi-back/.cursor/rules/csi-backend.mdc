---
description: CSI 后端开发规则（虚拟环境、依赖、测试、目录结构、工具函数与代码规范）
alwaysApply: true
---

# CSI 后端开发规则

## 虚拟环境与运行调试

- 所有运行、调试、测试必须在 **venv 虚拟环境** 下进行（本项目使用 `.venv`）。
- 执行任何 Python 相关命令前，先检查虚拟环境是否存在（如 `.venv` 目录或 `python -c "import sys; print(sys.prefix)"` 是否在 venv 内）。
- 若虚拟环境不存在，先创建：`python -m venv .venv`，再激活并安装依赖（生产：`pip install .`；本地开发含测试：`pip install -e .[dev]`），不得在未激活 venv 的情况下直接执行项目代码。

## 依赖管理

- 修改时**优先使用项目已安装的依赖**（参考 `requirements/` 下清单与 `pyproject.toml`）。
- **若必须引入新依赖**：在任何情况下都**先向用户确认**，列出拟添加的包及用途，**不得直接执行** `pip install` 或修改依赖文件。

## 测试与交付

- **新增或实质性修改功能**（接口、服务、工具函数、中间件等）后，必须补充**对应的 pytest 用例**；用例放置位置与命名需与现有 `tests/` 结构一致（例如工具类对应 `tests/utils/`）。
- 完成开发与用例编写后，须在 **`.venv` 内完整执行一次全量测试**（例如 `python -m pytest tests`），**全部通过**后方可视为本阶段完成；若有失败，应先修复再继续后续工作或收尾。

## 目录结构与一致性

- 执行修改前**先弄清项目文件与目录结构**。
- 新增文件或模块时，放置位置、命名、导入路径需与现有结构一致，保持**结构一致性**。

## 项目结构与工具

- 在进行具体改动前，**先了解项目结构**，确保代码放在正确的路径和文件中。
- 工具函数统一放在 **utils 工具目录**。
- 创建新工具函数前，**先检查是否已有可复用的工具函数**，避免重复实现。

## 代码质量

- 避免冗余代码，保持逻辑清晰、代码精简。
- 便于维护和扩展；修改时考虑现有模块的职责边界与复用性。
