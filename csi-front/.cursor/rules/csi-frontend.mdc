---
description: CSI 前端开发规则（依赖、样式、Vue 组合式与 script setup、项目结构与代码质量）
alwaysApply: true
---

# CSI 前端开发规则

## 依赖

- 所有修改尽可能使用项目中已安装的依赖。
- 若必须引入新依赖，**在任何情况下都先向用户确认**，不得直接执行安装。

## 样式与布局

- 优先使用 **Tailwind CSS v4** 实现样式和布局。
- 尽可能减少或避免自定义 CSS；仅在 Tailwind 无法满足或依赖有特殊要求时再写自定义样式。

## Vue 风格

- 默认使用 **组合式 API**（Composition API）与 **`<script setup>`** 语法。
- 尽可能不使用选项式 API（Options API），若必须使用，应提前告知，并在对应的文件开头以注释的方式写入必须使用选项式API的原因。

## 项目结构与工具

- 在进行具体改动前，**先了解项目结构**，确保代码放在正确的路径和文件中。
- 工具函数统一放在 **utils 工具目录**。
- 创建新工具函数前，**先检查是否已有可复用的工具函数**，避免重复实现。

## 代码质量

- 避免冗余代码，保持逻辑清晰、代码精简。
- 便于维护和扩展；修改时考虑现有模块的职责边界与复用性。
